#!/usr/bin/python
#-*- coding: utf-8 -*-

from odoo import models, fields, api, _

class sh_git_commit(models.Model):

    #_name = "sh.git_commit"
    _description = "sh.git_commit"

    _inherit = "sh.git_commit"